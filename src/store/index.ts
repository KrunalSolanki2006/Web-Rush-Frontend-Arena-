/**
 * @module store
 * @description Application state store barrel export.
 * The store layer manages global UI state (receipt drawer, navigation)
 * using the React Context + useReducer pattern.
 */
export * from './receiptStore';
